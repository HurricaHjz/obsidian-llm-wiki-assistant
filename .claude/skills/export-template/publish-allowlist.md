# Publish-gate allowlist — adjudicated-benign matches

Lines the personal-strings probe of `publish_guard.py` may ignore, each with its one-line justification. The probe parses this file, so its format is executable, not decorative.

**Format** — `- <key> :: <where it matches> :: <why benign>`, one entry per line.
- **Key**: either the literal string, when the string is itself public-safe, or a placeholder standing for a derivation class, when it is not. The placeholders are `<agent name>` · `<repo owner handle>` · `<licence holder's legal name>` · `<owner email token>` · `<machine account name>` · `<vault path component>` · `<extra gate string>`; anything else in angle brackets halts the gate. A placeholder covers a needle whenever that class is among the classes the needle was derived from, and a needle can carry several.
- **Where**: every file the entry covers, each in backticks, payload-relative. An entry that names no file halts the gate: an allowance floating free of its files would let a string spread from the line it was adjudicated for into any file that later quoted it, and still report clean.
- **Counts**: a **provisional** entry — an owner-ruled deferral of a class fix to a named release — writes `PROVISIONAL, expires at vX.Y` in its Where field and a per-file count after each path, `` `path` (N) ``. **N counts occurrences, not matching lines.** A permanent entry carries no counts and is uncapped inside its files. A hit outside an entry's files, or above a provisional count, stays unallowlisted and halts the publish.
- **Expiry**: the release named in a provisional entry removes that entry; `publish_guard.py --release vX.Y` turns a reached expiry into a gate failure, and every run reports the pending ones.

**Rules** — an entry describes the framework line that matches and never quotes personal data; entries must themselves be public-safe, because this file ships with the skill; add one only after a real adjudication, once, permanently. Entries the probe reports as *inert* matched no needle this run: they are kept, not deleted, since a retired string can return.

- OneDrive :: `CLAUDE.md` :: names a sync service generically as a drift cause; no path or account data; public since v0.6.x (adjudicated 2026-07-21)
- OneDrive :: `.claude/skills/lint/SKILL.md` — the same generic drift phrase in "When to run" :: identical wording and rationale as the CLAUDE.md entry (adjudicated 2026-07-22)
- <repo owner handle> :: `README.md` and `.claude/skills/export-template/RUNBOOK.md` — the clone and remote URLs :: the public repository's own address, which cannot be concealed from itself; contains no private data (adjudicated 2026-07-22)
- <licence holder's legal name> :: `LICENSE.md` — the copyright line — and `.claude/skills/export-template/SPEC.md`, whose tree annotation describes the MIT licence :: the owner is the public MIT licensor, and a licence requires the licensor's name — a deliberate owner decision at v0.1 (log 2026-06-24, "name only in the LICENSE"); no other personal data (adjudicated 2026-07-23)
- Library :: `README.md`, `.claude/skills/ingest/SKILL.md`, `.claude/skills/adopt/adopt_fingerprint.py`, `.claude/skills/delegate/lane-fence.py`, `.claude/skills/delegate/test_lane.sh`, `.claude/skills/gather/test_gather_links.py` :: an ordinary English word that is also a component of this vault's path, so the derivation yields it; every match is the word, and the component identifies nobody on its own (adjudicated 2026-09-06)
- <agent name> :: PROVISIONAL, expires at v1.0 — the agent-name-prefixed environment variables, four default paths and a few prose mentions in `.claude/skills/delegate/handsoff.py` (37), `.claude/skills/delegate/test_handsoff.sh` (59), `.claude/skills/delegate/SKILL.md` (7), `.claude/skills/delegate/fable-share.py` (2), `.claude/skills/delegate/test_fable_share.sh` (1), `setup.sh` (1), `.claude/skills/delegate/lane.py` (3), `.claude/skills/delegate/test_lane.sh` (19), `.claude/skills/delegate/test_watch.sh` (28), `.claude/skills/delegate/watch.py` (10), `.claude/skills/project/SKILL.md` (3), `MANUAL.md` (1) and `.claude/skills/export-template/payload/setup.sh` (1) :: the agent's name is user-space config (`agent_name` in CUSTOMISATION), not personal data; the rename to a name-neutral prefix with a dual read is deferred to v1.0 by owner ruling 2026-09-06 (known-issues, the 2026-09-06 name entry), and that release removes this line. While the class is deferred the counts track the files' growth and are refreshed at each publish from the gate's own over-cap finding; the file list is what bounds the spread. Extended 2026-09-08 on the owner's ruling ("extend, yes") to six console and project files carrying the same variable class.
